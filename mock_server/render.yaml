services:
  - type: web
    name: tymex-market-place-nnhg
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn mock_server.main:app --host 0.0.0.0 --port 10000"
    plan: free
    envVars:
      - key: PORT
        value: 10000